// This file is intentionally minimal.
// The previous OAuth-based exports (COOKIE_NAME, ONE_YEAR_MS, getLoginUrl)
// were removed when authentication moved to email/password + JWT.
// Do not re-add them without also updating server/_core/auth.ts and the auth routes.
export {};
